apiVersion: v1
kind: Service
metadata:
  name: pursit-back
  namespace: pursit
spec:
  selector:
    app: pursit-back
  ports:
    - protocol: TCP
      port: 80
      name: http
      targetPort: 8080
  type: ClusterIP
